// 思路：用整除和余数把总秒数依次拆分为小时、分钟和秒。
#include <stdio.h>
int main()
{
    int t, h = 0, m = 0, s = 0;
    scanf("%d", &t);
    h = t / 3600; // 整除 3600 得到完整的小时数。
    m = (t - 3600 * h) / 60;
    s = t - 3600 * h - 60 * m;
    printf("%d:%d:%d", h, m, s);
    return 0;
}
// 总结：时间换算可以逐级取整，再从剩余量中继续分解。